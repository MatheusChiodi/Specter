{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissões base do Specter (launcher + panel)",
  "windows": ["launcher", "panel"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
