import type { JSX } from "react";
import Panel from "./components/Panel";

/** App da janela `panel` — compõe o painel principal. */
export default function App(): JSX.Element {
  return <Panel />;
}
